<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>login</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-stone-600 flex items-center justify-center h-screen">

    <div class="bg-zinc-900 p-6 rounded-lg shadow-lg w-full max-w-md">
        <a class="bg-red-800 rounded-md px-3 py-2 mb-6" href="/logout">Logout</a>
        <h2 class="text-2xl text-white font-bold mb-6 text-center">Hello! <%= user.name %></h2>
        <h5 class="mb-6">Create a Post</h5>
        <form class="mb-6" action="/post" method="post">
            <textarea class=" w-full resize-none bg-transparent border-2 border-zinc-1s00" name="content" id=""></textarea>
            <button type="submit" class="w-full py-2 px-4 bg-indigo-600 text-white rounded-md hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-indigo-500">Create New Post</button>
        </form>
        <div class="bg-zinc-900 p-6 rounded-lg shadow-lg w-full max-w-md">
            <h3 class="text-zinc-400">Your Posts</h3>
            <div class="postContainer">
                <% user.posts.reverse().forEach((post) => { %>
                    <div class="post mb-3 w-full p-5 border-2 border-zinc-800 ">
                        <h4 class="text-blue-500 mb-3">@<%= user.username  %></h4>
                        <p class="text-sm text-white tracking-tight"><%= post.content  %></p>
                        <div class="btns flex gap-4 mt-5">
                            <a href="/like/<%= post._id  %>" class="text-blue-500">Like</a>
                            <a href="" class="text-zinc-600">Edit</a>
                        </div>
                    </div>
                <% }) %>
            </div>
        </div>
    </div>

</body>
</html>
